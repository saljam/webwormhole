# Built web assets (generated by `make wasm`)
/web/webwormhole.wasm
/web/wasm_exec.js

# Local CLI / server binaries
/ww
/ww.exe
*.exe

# TLS / Let's Encrypt cache used by `ww server -secrets ...`
/keys/
*.pem
*.key
*.crt

# Env / local secrets (if you add any later)
.env
.env.*
!.env.example

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/
*.log
